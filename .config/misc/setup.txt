# SSH Stuff
ssh-keygen -t rsa -b 4096 -C "dantedigdog@gmail.com"
eval "$(ssh-agent -s)"
ssh-add ~/.ssh/id_rsa
cat ~/.ssh/id_rsa.pub

# Clone repo and set it up
git clone git@github.com:DanteDogDev/dotfiles.git dotfiles --bare 
/usr/bin/git --git-dir=$HOME/dotfiles/ --work-tree=$HOME config --local status.showUntrackedFiles no
/usr/bin/git --git-dir=$HOME/dotfiles/ --work-tree=$HOME checkout --force

